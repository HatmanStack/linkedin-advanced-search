# Docker-specific environment overrides
# These are used by docker-compose services

# Frontend (Vite)
VITE_API_URL=http://localhost:3001
VITE_COGNITO_USER_POOL_ID=local_test_pool
VITE_COGNITO_USER_POOL_WEB_CLIENT_ID=local_test_client
VITE_COGNITO_REGION=us-east-1

# Puppeteer backend
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
LINKEDIN_BASE_URL=http://mock-linkedin:3333
DYNAMODB_TABLE_NAME=linkedin-advanced-search-test
S3_BUCKET_NAME=linkedin-screenshots-test

# LocalStack
AWS_ENDPOINT_URL=http://localstack:4566
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_DEFAULT_REGION=us-east-1
